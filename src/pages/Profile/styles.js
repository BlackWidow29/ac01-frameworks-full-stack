import styled from "styled-components";
import { FlexDiv } from "../../components/base";

export const Background = styled(FlexDiv)`
  height: 100%;
  width: 100%;
  background: var(--tertiary);
`;

export const Box = styled(FlexDiv)`
  background: white;
  padding: 30px;
`;